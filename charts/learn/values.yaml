image: dxas90/learn:v0.0.11

imageConfig:
  pullPolicy: IfNotPresent

replicas: 1

labels:
  env: "local"
  cluster: "eks-cluster-blog"

resources:
  limits:
    cpu: 1m
    memory: 56Mi
  requests:
    cpu: 1m
    memory: 56Mi

service:
  port: 80
  targetPort: 8080
  type: ClusterIP
